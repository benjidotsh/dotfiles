# shellcheck shell=sh
{{ if eq .profile "work" -}}
{{- $artifactory := onepasswordRead "op://dotfiles/Secrets/Artifactory" | trim -}}
export GH_TOKEN={{ onepasswordRead "op://dotfiles/Secrets/GitHub DPG Media" | trim | shellQuote }}
export ARTIFACTORY_API_KEY={{ $artifactory | shellQuote }}
export GOPROXY={{ printf "https://bejanssens:%s@artifactory.persgroep.cloud/artifactory/api/go/go,https://proxy.golang.org,direct" $artifactory | shellQuote }}
{{ else -}}
export GH_TOKEN={{ onepasswordRead "op://dotfiles/Secrets/GitHub" | trim | shellQuote }}
{{ end -}}
