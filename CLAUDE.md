## Agent skills

### Issue tracker

Issues live in GitHub at `lvthillo/dotfiles`. See `docs/agents/issue-tracker.md`.

### Triage labels

Uses the five default triage labels. See `docs/agents/triage-labels.md`.

### Domain docs

Uses the single-context layout. See `docs/agents/domain.md`.
